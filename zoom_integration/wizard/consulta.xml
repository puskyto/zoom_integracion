<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_consulta_compute_wizard" model="ir.ui.view">
        <field name="name">Consulta Zoom</field>
        <field name="model">generar.consulta.zoom</field>
        <field name="arch" type="xml">
            <form string="Parameters">
                <p>
                The stock will be reserved for operations waiting for availability and the reordering rules will be triggered.
                </p>
                <footer>
                    <button name="procure_calculation" string="Run Scheduler" type="object" class="btn-primary"/>
                    <button string="Cancel" class="btn-secondary" special="cancel" />
                </footer>
            </form>
        </field>
    </record>

    <record id="action_consulta_compute" model="ir.actions.act_window">
        <field name="name">Run Scheduler consulta</field>
        <field name="res_model">generar.consulta.zoom</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>

    <menuitem action="action_consulta_compute" id="menu_consulta_compute" parent="stock.menu_stock_root" sequence="135"/>


</odoo>
